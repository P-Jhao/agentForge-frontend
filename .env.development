# 开发环境配置

# API 基础路径
VITE_API_BASE=/api

# 代理配置：[[前缀, 目标地址], ...]
# /api - API 接口
# /defaultImgs - 默认头像等静态资源
# /uploads - 用户上传的文件
VITE_PROXY=[["/api", "http://localhost:3000"], ["/defaultImgs", "http://localhost:3000"], ["/uploads", "http://localhost:3000"]]
